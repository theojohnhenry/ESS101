function x_next = rk_step(f,x,dt,a,b,c)
    %tar in dynamics
    %nuvarnade state
    %timestep
    %a b c konstanter i butcher

    s = length(b);
    K = [f(x)]

    %make k vector
    for i=2:s
        sum = 0;
        for j=1:s
            sum = sum + a(i,j)*K(j);
        end
        K{i) = f(x + dt * sum);
    end

    jonny=0;
    for i=1:s
        jonny=jonny+b(i)*K{i};
    end

    x_next = x+dt*jonny;
end